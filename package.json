{
  "name": "@jackcom/app-network-layer",
  "version": "1.0.0",
  "description": "A lightweight client-side server interface. Provides a single point for implementing global promise error-handling in a Javascript application",
  "main": "lib/index.js",
  "files": [
    "/lib"
  ],
  "scripts": {
    "test": "jest --watchAll --testTimeout=300000 --detectOpenHandles",
    "build": "rm -rf lib/ && tsc",
    "build:preview": "clear && npm run build && npm run preview",
    "preview": "npm pack && rm -rf *.tgz"
  },
  "keywords": [
    "request",
    "layer",
    "network",
    "layer"
  ],
  "author": "JACKCOM",
  "repository": {
    "type": "git",
    "url": "https://github.com/JACK-COM/NetworkLayer.git"
  },
  "license": "WTFPL",
  "devDependencies": {
    "@babel/preset-env": "^7.15.6",
    "@babel/preset-react": "^7.14.5",
    "@babel/preset-typescript": "^7.15.0",
    "@types/jest": "^27.0.1",
    "jest": "^27.2.0",
    "npm-auto-version": "^1.0.0",
    "ts-loader": "^9.2.5",
    "typescript": "^4.4.3",
    "webpack": "^5.52.1",
    "webpack-cli": "^4.8.0"
  },
  "dependencies": {
    "axios": "^0.27.2"
  }
}
